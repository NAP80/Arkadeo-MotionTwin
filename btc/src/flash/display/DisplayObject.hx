package flash.display;

// flash.display.DisplayObject = base PixiJS. Nos Sprite/Bitmap/Graphics/TextField
// en héritent, donc tout objet de rendu est un DisplayObject PixiJS.
typedef DisplayObject = pixi.core.display.DisplayObject;
