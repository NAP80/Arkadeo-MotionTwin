package mt.deepnight.mui;

// Stub mui : la vraie hiérarchie (Group/Window/Label…) est trop couplée Flash. On ne
// garde que ce que BTC nomme (updateAll, + Window/Label pour le tuto Progression).
class Component {
	public static function updateAll():Void {}
}
